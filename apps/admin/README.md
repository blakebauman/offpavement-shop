# Off-Pavement Admin

Medusa admin customizations live in `packages/medusa/src/admin`. This directory is reserved for future admin extensions (e.g. custom widgets, kit builder management UI).

Run the admin via: `pnpm dev:medusa` — admin is served at http://localhost:9000/app
